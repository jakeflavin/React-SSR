import styled from 'styled-components';

const CountDownHeaderStyled = styled.span`
  flex: 0 0 100%;
  text-align: center;
  font-weight: 800;
  font-size: 1.5rem;
`;

export default CountDownHeaderStyled;
